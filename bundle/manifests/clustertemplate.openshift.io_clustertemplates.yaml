apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.9.0
  creationTimestamp: null
  name: clustertemplates.clustertemplate.openshift.io
spec:
  group: clustertemplate.openshift.io
  names:
    kind: ClusterTemplate
    listKind: ClusterTemplateList
    plural: clustertemplates
    shortNames:
    - ct
    - cts
    singular: clustertemplate
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - description: Cluster cost
      jsonPath: .spec.cost
      name: Cost
      type: integer
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Template of a cluster - defines resources required to start the
          installation (via Helm chart) and optionally a post install setup of the
          cluster (via Tekton pipeline)
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            properties:
              clusterSetup:
                properties:
                  pipeline:
                    properties:
                      name:
                        type: string
                      namespace:
                        type: string
                    required:
                    - name
                    - namespace
                    type: object
                  pipelineRef:
                    description: PipelineRef can be used to refer to a specific instance
                      of a Pipeline.
                    properties:
                      apiVersion:
                        description: API version of the referent
                        type: string
                      bundle:
                        description: Bundle url reference to a Tekton Bundle.
                        type: string
                      name:
                        description: 'Name of the referent; More info: http://kubernetes.io/docs/user-guide/identifiers#names'
                        type: string
                      resolver:
                        description: Resolver is the name of the resolver that should
                          perform resolution of the referenced Tekton resource, such
                          as "git".
                        type: string
                      resource:
                        description: Resource contains the parameters used to identify
                          the referenced Tekton resource. Example entries might include
                          "repo" or "path" but the set of params ultimately depends
                          on the chosen resolver.
                        items:
                          description: ResolverParam is a single parameter passed
                            to a resolver.
                          properties:
                            name:
                              description: Name is the name of the parameter that
                                will be passed to the resolver.
                              type: string
                            value:
                              description: Value is the string value of the parameter
                                that will be passed to the resolver.
                              type: string
                          required:
                          - name
                          - value
                          type: object
                        type: array
                        x-kubernetes-list-type: atomic
                    type: object
                type: object
              cost:
                minimum: 0
                type: integer
              helmChartRef:
                properties:
                  name:
                    type: string
                  repository:
                    type: string
                  version:
                    type: string
                required:
                - name
                - repository
                - version
                type: object
              properties:
                items:
                  description: TODO add admission webhook
                  properties:
                    clusterSetup:
                      description: prop Type can be string only
                      type: boolean
                    defaultValue:
                      x-kubernetes-preserve-unknown-fields: true
                    description:
                      type: string
                    name:
                      type: string
                    overwritable:
                      type: boolean
                    secretRef:
                      properties:
                        name:
                          type: string
                        namespace:
                          type: string
                      required:
                      - name
                      - namespace
                      type: object
                    type:
                      enum:
                      - string
                      - bool
                      - int
                      - float
                      type: string
                  required:
                  - description
                  - name
                  - overwritable
                  - type
                  type: object
                type: array
            required:
            - cost
            type: object
          status:
            description: ClusterTemplateStatus defines the observed state of ClusterTemplate
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions: null
